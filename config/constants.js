// Global Constants
// This file defines fixed values used throughout the application,
// such as conversation states for the bot.

const STATES = {
    WELCOME: 'welcome',
    BROWSE_HOTELS: 'browse_hotels',
    SELECT_HOTEL: 'select_hotel',
    SELECT_DATES: 'select_dates',
    CONFIRM_BOOKING: 'confirm_booking',
    PAYMENT: 'payment',
    COMPLETED: 'completed'
};

module.exports = {
    STATES
};
